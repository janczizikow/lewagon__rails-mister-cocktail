<%= link_to cocktail_path(id), class: 'card my-3' do  %>
  <% unless photo.blank? %>
    <%= cl_image_tag photo, alt: name, class: 'card-img-top' %>
  <% end %>
  <div class="card-body">
    <h5 class="card-title"><%= name %></h5>
    <p class="card-description"><%= description %></>
  </div>
<% end %>
